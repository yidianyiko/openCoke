## 原因分析
- 输入服务工作正常：`connector/ecloud/ecloud_input.py:153-160` 将来消息标准化并写入 `inputmessages`；日志显示两条消息被接收并返回 200。
- 算法层未产出回复：`qiaoyun/runner/qiaoyun_runner.log:110-299` 多个 Agent 报错（JSON 解析失败、上下文为空导致 `'NoneType'`），主聊天链路在 `qiaoyun/agent/qiaoyun_chat_agent.py:61` 访问 `result["resp"]["MultiModalResponses"]` 时即失败，未调用 `send_message_via_context(...)`，因此没有写入 `outputmessages`。
- 发送器没机会发送：`connector/ecloud/ecloud_output.py:22-37` 仅轮询 `status="pending"` 且到时的 `outputmessages`；日志中未出现“sending...”，佐证没有待发消息。
- 配置基本就绪：`conf/config.json:10-15` 已配置 `Authorization` 与 `wId.qiaoyun`；若生成 `outputmessages`，发送链路会用别名 `character["name"]` 映射 `wId`。

## 验证与联调计划
- 步骤1（服务确认）：确保已运行输入、输出与算法层脚本，分别观察 `connector/ecloud/ecloud.log` 与 `qiaoyun/runner/qiaoyun_runner.log` 有持续输出。
- 步骤2（直接写待发消息进行联调）：用 Python 调用 `qiaoyun/util/message_util.py:139-167` 的 `send_message(...)`，插入一条 `platform="wechat"`、`from_user=角色ID`、`to_user=一点一口用户ID`、`message_type="text"` 的记录，`expect_output_timestamp=now`。
  - 角色ID可由 `qiaoyun/runner/qiaoyun_handler.py:52-58` 的查询获得；日志中显示角色 `_id=69174a09005d9a476b7729ad`。
  - 用户ID可由输入消息记录获得；日志显示一点一口 `_id=6917593b503f6b8f3de12c72`。
  - 插入后，观察 `connector/ecloud/ecloud.log` 是否出现 `sending...`、`resp_json`。
- 步骤3（直连API兜底验证）：如需绕过数据库，调用 `connector/ecloud/ecloud_api.py:14-40, 62-96` 的 `Ecloud_API.sendText(data)`，`data={"wId": CONF["ecloud"]["wId"]["qiaoyun"], "wcId": 一点一口的微信号account, "content": "测试短信"}`；账号 `account` 可从用户文档 `platforms.wechat.account` 读取或通过 `Ecloud_API.getContact(...)` 获取。
- 步骤4（修复自动回复链路）：
  - 排查并修复 `QiaoyunQueryRewriteAgent` 的函数调用风格 JSON 解析（`framework/agent/llmagent/base_singleroundllmagent.py:203-208`）；确认上游模型与提示词输出严格为 JSON。
  - 修复上下文模板注入导致的 `'NoneType'`（`framework/agent/llmagent/base_singleroundllmagent.py:121-130`）与 `QiaoyunContextRetrieveAgent` 的空结果处理（`qiaoyun/agent/qiaoyun_context_retrieve_agent.py:38`），确保 `result["resp"]` 不为 `None`。
  - 在 `qiaoyun/runner/qiaoyun_handler.py` 增加失败兜底：当聊天链路报错或返回空时，使用 `send_message_via_context(...)` 发送一条简短文本，保证用户侧感知到回复；同时将错误写入日志，便于后续定位。

## 执行细节（待确认后实施）
- 我将先用 `send_message(...)` 插入一条到“一点一口”的测试消息，验证 `ecloud_output` 能成功发出，并在日志中确认返回码为 `code=1000`。
- 若直发成功，再着手修复聊天链路的 JSON 解析与上下文为空问题，并补充失败兜底回复，恢复自动化对话。
- 如遇 `wId` 或 `account` 缺失，将读取数据库中的角色与用户文档或调用 `getContact(...)` 获取并比对。