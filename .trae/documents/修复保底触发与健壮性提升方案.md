## 目标
- 手动确认是哪个接口在返回空结构化结果（`{}`）：
- 聚焦 Doubao 的 `chat.completions` 调用（QueryRewrite/ChatResponse 两处）。

## 测试方法 A（直连 Ark/Doubao）
- 直接使用 `volcenginesdkarkruntime.Ark` 调用 `chat.completions`，带上 QueryRewrite 的 `default_output_schema` 作为 `tools` 与 `tool_choice`，观察原始返回：
- 关键打印：`choices[0].message.tool_calls[0].function.arguments`、`choices[0].message.function_call.arguments`、`choices[0].message.content`、完整 `response` 的片段。
- 代码（可在新终端运行，需确保 `ARK_API_KEY` 已设置）：
```
python3 - << 'PY'
from volcenginesdkarkruntime import Ark
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as QA
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as Q
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as Agent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as A
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as QQ
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as QRA
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as X
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as Y
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as Z
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as R
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as T
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as U
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent as V
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
from volcenginesdkarkruntime import Ark
client = Ark(base_url="https://ark.cn-beijing.volces.com/api/v3")
from qiaoyun.agent.qiaoyun_query_rewrite_agent import QiaoyunQueryRewriteAgent
schema = QiaoyunQueryRewriteAgent.default_output_schema
systemp = QiaoyunQueryRewriteAgent.default_systemp_template
userp = QiaoyunQueryRewriteAgent.default_userp_template
ctx = {
  "conversation": {"conversation_info": {
    "time_str": "2025年11月15日20时42分 星期六",
    "chat_history_str": "（2025年11月15日19时41分 一点一口发来了文本消息）为什么你不理我\n（2025年11月15日19时41分 一点一口发来了文本消息）哈喽",
    "input_messages_str": "（2025年11月15日20时42分 一点一口发来了文本消息）现在好了吗"
  }},
  "character": {"user_info": {"description": "INTJ，自信、擅长心理洞察"}, "platforms": {"wechat": {"nickname": " coke "}}},
  "relation": {"relationship": {"description": "在微信上认识的新朋友", "closeness": 20, "trustness": 20, "dislike": 0, "status": "空闲"},
               "character_info": {"longterm_purpose": "慢慢挑选合适男友", "shortterm_purpose": "少量闲聊", "attitude": "略微好奇"},
               "user_info": {"realname": "", "hobbyname": "", "description": "在微信上认识的新朋友"}},
  "news_str": ""
}
sys_p = systemp.format(**ctx)
usr_p = userp.format(**ctx)
functions = [{"type":"function","function":{"name":"json_format_response","description":"The response using well-structured JSON.","parameters":schema}}]
function_call = {"type":"function","function":{"name":"json_format_response"}}
resp = client.chat.completions.create(model="ep-20241114003058-czods",messages=[{"role":"system","content":sys_p},{"role":"user","content":usr_p}],tools=functions,tool_choice=function_call)
m = resp.choices[0].message
print("TOOL_CALLS:", getattr(m, "tool_calls", None))
print("FUNCTION_CALL:", getattr(m, "function_call", None))
print("CONTENT:", getattr(m, "content", None))
PY
```
- 期望输出：若 `arguments == "{}"` 或 `content == "{}"`，即可确认为空来源。

## 测试方法 B（经 Agent 层）
- 用 `QiaoyunQueryRewriteAgent(context)` 直接 `run()`，打印 `agent.context["llm_response"]` 的同字段，确认解析路径：
- 关键打印：`tool_calls.function.arguments`、`function_call.arguments`、`message.content`、`result["resp"]`。

## 测试方法 C（对比 ChatResponse）
- 同样方式调用 `QiaoyunChatResponseAgent`，验证是否也会返回空结构；用于排除模板或上下文导致的异常。

## 交付与判定
- 给出每个方法的原始片段与结论：空来源是 `tool_calls.function.arguments` 还是 `message.content`；对应接口均为 Doubao `chat.completions`。
- 若仅 QueryRewrite 为空而 ChatResponse 非空，优先修正 QueryRewrite 的 schema 与提示模板；若二者都为空，按模型与提示层面逐项修复。