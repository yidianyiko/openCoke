#!/usr/bin/env zsh

set -euo pipefail

root="$(cd "$(dirname "$0")/.." && pwd)"
cd "$root"

python_cmd="python3"
if [[ -x ".venv/bin/python" ]]; then
  python_cmd=".venv/bin/python"
fi

exec "$python_cmd" scripts/guardrails.py suggest-verification "$@"
