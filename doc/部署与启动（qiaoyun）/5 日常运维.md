## 本地运维
- 定期清理本地临时目录（这里基本上）
```
rm -rf qiaoyun/temp/*
```
- 定期重启ecloud connector（目前不知道是因为什么，有时候会卡死收不到消息）
```
source qiaoyun/runner/qiaoyun_start.sh
```
## 云存储清理
- 定期清理云存储空间

## 定期充钱


## 查看提醒的数据库
python -c "import sys; sys.path.append('.'); from dao.reminder_dao import ReminderDAO; dao=ReminderDAO(); items=list(dao.collection.find({'status':'confirmed'}).sort('next_trigger_time',1)); print('count:', len(items)); [print(i.get('user_id'), i.get('title'), i.get('status'), i.get('next_trigger_time')) for i in items[:20]]; dao.close()"

#删除数据
python qiaoyun/runner/del-data-role.py wxid_58bfckbpioh822

## 清空数据库

mongosh

use mymongo

# 清空所有集合的记录，保留集合结构和索引
db.getCollectionNames().forEach(c => {
    db[c].deleteMany({})
    print("已清空: " + c)
})

# 删除指定关键词提醒

source .venv/bin/activate && python3 -c "
import sys
sys.path.append('.')
from pymongo import MongoClient

client = MongoClient('mongodb://127.0.0.1:27017/')
db = client['mymongo']
collection = db.reminders

query = {
    '\$or': [
        {'title': {'\$regex': '', '\$options': 'i'}},
        {'action_template': {'\$regex': '准备', '\$options': 'i'}}
    ]
}

# 先查看有多少条
count = collection.count_documents(query)
print(f'找到 {count} 条准备相关记录')

# 删除所有
result = collection.delete_many(query)
print(f'✓ 已删除 {result.deleted_count} 条记录')

client.close()
"
